# =====================================
# ARQUIVO SECRETO (PRODUÇÃO) — EXEMPLO
# Preencha apenas em ambiente seguro (Vercel/servidor)
# NUNCA versionar um arquivo .env.production real
# =====================================

# Banco de Dados (PostgreSQL)
DATABASE_URL=

# Autenticação (>= 32 chars)
AUTH_SECRET=

# URL pública do app (ex.: https://seuapp.com)
APP_URL=

# Groq (IA real)
# Cole o valor real no ambiente (Vercel/servidor) OU use .env.production.local (não versionado)
GROQ_API_KEY=
# Opcional: sobrescreve o modelo padrão usado em services/groq.ts
GROQ_MODEL=

# Mercado Pago (planos recorrentes)
# Opção A: via env (mais simples)
MERCADOPAGO_ACCESS_TOKEN=
# Opção B: via Admin → /admin/settings (salvo criptografado no banco)
# (neste caso, o env acima pode ficar vazio/removido)

# (OPCIONAL) Rate limit distribuído (Upstash Redis)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# (OPCIONAL) Billing webhook (se habilitar pagamentos)
# BILLING_WEBHOOK_TOKEN=
