export default function Home() {
  return (
    <div>
      <h1>Neon Auth — Example</h1>
      <p>Use <code>/auth</code> to request a magic link and <code>/account</code> to view the active session.</p>
    </div>
  );
}
