# Exemplo de config para scripts/guardian.sh
# Recomendado: colocar em /etc/signalhacker/guardian.env e exportar GUARDIAN_ENV_FILE apontando pra ele.

# App
APP_BASE_URL="http://localhost:3000"
HEALTH_PATH="/api/health"
DEBUG_DB_PATH="/api/debug-db"

# Docker Compose
COMPOSE_DIR="/srv/signalhacker"
COMPOSE_FILE="/srv/signalhacker/docker-compose.yml"
APP_SERVICE="app"
DB_SERVICE="db"

# Logs
LOG_DIR="/srv/signalhacker/logs"
LOG_FILE="/srv/signalhacker/logs/guardian.log"
LOCK_FILE="/tmp/signalhacker_guardian.lock"

# Backup (Postgres)
BACKUP_DIR="/srv/signalhacker/backups"
BACKUP_RETENTION_DAYS=30
# IMPORTANT: configure como secret em produção
BACKUP_KEY="CHANGE_ME_LONG_RANDOM"

# Postgres (dentro do container)
POSTGRES_DB="edgemind"
POSTGRES_USER="postgres"

# Alertas (opcional)
# WHATSAPP_NUMBER="55XXXXXXXXXXX"

# TLS check (opcional)
# TLS_HOST="api.seudominio.com"
# TLS_PORT=443
